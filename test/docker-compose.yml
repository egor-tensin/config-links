version: '3'
services:
  test:
    build:
      context: docker/
      args:
        DISTRO: "${DISTRO:-xenial}"
    volumes:
      - ../:/src
    command:
      - sh
      - -c
      - /src/test/local/test.sh && /src/test/linux-home/test.sh
